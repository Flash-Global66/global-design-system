*.log
.DS_Store
node_modules
dist
**/dist
.codegpt
.yarn/
remote_versions.txt
registry_versions.txt

.npmrc
.env

backups-temporal/

# Build output
storybook-static/


# Playwright
.playwright-mcp/

# yalc
.yalc/

# agents
.pi/
.atl/

# Font Awesome vendor dir (generated by icon-font/scripts/ensure-fa.mjs)
.fa-vendor/

# Claude/agents local config (skills, settings) — generado por install-skills
.claude/
.agents/

# Agent local configuration — credenciales del dev (ver agent.local.json.example)
agent.local.json

# MCP servers — generado por `yarn install-skills` desde ai-context. No editar a mano.
.mcp.json

# agent-browser — config, secrets y evidencia generados por resolve-config.mjs
.agent/
