# Tasks: ep-extraction-v6 — Close out the element-plus removal

## Review Workload Forecast

| Field                   | Value                                                                    |
| ----------------------- | ------------------------------------------------------------------------ |
| Estimated changed lines | ~2,150–3,600 total across 12 units (see per-unit table)                  |
| 400-line budget risk    | High                                                                     |
| Chained PRs recommended | Yes                                                                      |
| Suggested split         | WU1 → WU2 → ... → WU12, one PR per work unit, in design order            |
| Delivery strategy       | ask-on-risk (orchestrator asks user before apply)                        |
| Chain strategy          | pending — user decision needed (stacked-to-main vs feature-branch-chain) |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Per-Unit Estimate

| WU  | Unit                                                        | Est. lines | Risk     | Notes                              |
| --- | ----------------------------------------------------------- | ---------- | -------- | ---------------------------------- |
| 1   | badge                                                       | 150–250    | Low      | self-contained                     |
| 2   | radio-group                                                 | 200–350    | Low-Med  | + latent bug fix                   |
| 3   | skeleton + skeleton-item                                    | 250–400    | Med      | 2 components, near budget          |
| 4   | infinite-scroll                                             | 150–250    | Low      | new directive, isolated            |
| 5   | form-item                                                   | 250–400    | Med      | validation core coupling           |
| 6   | popover                                                     | 300–450    | Med-High | 3 composables re-pointed           |
| 7   | menu (×4)                                                   | 500–800    | **High** | exceeds 400 alone — see flag below |
| 8   | time-picker clearIcon default                               | 20–50      | Low      | 2-line prop change only            |
| 9   | config-provider (+ SCSS task 4.5 + time-picker `@use` drop) | 200–350    | Med      | gates root removal                 |
| 10  | root removal + g-hooks test cleanup + eslintrc final entry  | 50–150     | Low      |                                    |
| 11  | front-b2b 8× version bumps                                  | 20–60      | Low      | mechanical                         |
| 12  | front-b2b EP removal                                        | 10–30      | Low      | mechanical                         |

**Flag**: WU7 (menu family) is estimated at 500–800 changed lines even as its own isolated PR, above the 400-line budget by design (one shared context module + 4 components + provide/inject tests cannot be safely split without breaking the symbol-identity handshake, per Design Decision 1). Options for the user to decide: (a) accept `size:exception` for WU7 only, (b) split WU7 into parent (Menu + context module) then children (MenuItem/MenuSub/MenuItemGroup) as two chained sub-PRs sharing the WU7 branch, or (c) proceed as-is and rely on reviewer discretion. This is flagged, not decided, per `ask-on-risk`.

Every WU below includes, per unit convention (do not skip for any unit 1–9): strict-TDD test-first implementation, `yarn test:run` green, ESLint `excludedFiles` removal for that package, byte/behavior parity check, and mandatory live `yarn-link` validation in `../front-b2b` before marking the unit done.

## WU1: badge (PR 1)

- [x] 1.1 Write failing tests in `components/badge/tests/Badge.spec.ts` for native render (props/slots/emits parity vs current `ElBadge` usage) — 17 tests, RED confirmed against old EL-wrapped Badge.vue (revealed a pre-existing gap: outside a ConfigProvider it rendered `el-badge*`, not `gui-badge*`)
- [x] 1.2 Rewrite `components/badge/Badge.vue` as native `GBadge` template (remove `ElBadge` import) — uses `useNamespace('badge')` from `@flash-global66/g-utils`
- [x] 1.3 Port `components/badge/badge.styles.scss` to DS-owned styles (byte-exact parity vs current compiled CSS) — re-pointed `@use` to `@flash-global66/g-utils/{mixins,var-mixins,tokens}`, selectors/values preserved byte-exact; `$badge` token map already existed in g-utils/styles/tokens.scss from ep-extraction-scss
- [x] 1.4 Remove `components/badge/**` from `.eslintrc.cjs` excludedFiles; `yarn lint --max-warnings 0` green — also widened the guard's `files` glob to include `components/*/*.vue` (flat-layout root .vue files were not previously matched, so the exclude removal alone would have been a no-op for badge)
- [x] 1.5 `yarn test:run` green (17/17 badge, 362/362 full suite); yarn-link validate live in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge

## WU2: radio-group (PR 2)

- [x] 2.1 Write failing tests in `components/radio-group/tests/RadioGroup.spec.ts` for provide/inject (value/size/disabled) + native `role=radiogroup` render — 7 tests, RED confirmed against old code (module-resolution failure on the broken `../radio/radio.type` import, i.e. the latent bug itself); covers native render/class, slot passthrough, modelValue/disabled/size propagation via context, size default, change + update:modelValue emission, and a real `<g-radio>` (from `@flash-global66/g-radio`) nested-in-slot interop check
- [x] 2.2 Create `components/radio-group/radio.type.ts` defining `TypeRadioSize`/`TypeRadio`/`EnumRadioSize` — fixes latent missing-file bug from `RadioGroup.vue:15` (`../radio/radio.type` never existed). Investigated first: found the exact same type shape orphaned/dead in `components/popover/popover.type.ts` (unused there — nothing in popover imports it), confirming this was the intended content, just saved under the wrong package at some point. Recreated it in `radio-group`'s own package (per design's "own context key" / no cross-package coupling) rather than reintroducing a dependency on `../radio`; left the popover file untouched (out of WU2 scope, flagged as a risk below for whichever WU eventually owns popover cleanup)
- [x] 2.3 Rewrote `components/radio-group/RadioGroup.vue` as native `<div role="radiogroup">` (removed `ElRadioGroup`/`element-plus` import entirely) with its own `constants.ts` defining `radioGroupKey` via `Symbol.for('radioGroupKey')` — the same global-symbol-registry key string already used by `@flash-global66/g-radio`'s own `radio-group.vue` (`components/radio/src/constants.ts`), so a `<g-radio>` nested inside this legacy `<g-radio-group>` keeps resolving group context (modelValue/disabled/changeEvent) automatically with zero package dependency between the two islands (verified live in test 7). Added an explicit `modelValue` prop + `update:modelValue` emit (previously implicit via `$attrs` fallthrough onto `<el-radio-group>`) so v-model keeps working natively
- [x] 2.4 Removed `components/radio-group/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/radio-group/**/*.{ts,vue}'` clean (full-repo `yarn lint` has 163 pre-existing errors unrelated to this WU — confirmed identical count on main before this change minus 1, in unrelated packages: search-input, table, stories)
- [x] 2.5 `yarn test:run` green (362/362 full suite, 7/7 radio-group; only pre-existing unrelated `components/button/tests/Button.spec.ts` suite fails, blocked by an unbuilt `@flash-global66/g-icon-font` dist in this fresh worktree — confirmed pre-existing, not a regression). `npx vue-tsc --noEmit -p .` clean on all radio-group/radio production source files (RadioGroup.vue, constants.ts, radio.type.ts, `components/radio/**`); remaining vue-tsc errors on `RadioGroup.spec.ts` are the same pre-existing systemic jest-dom-matcher-type gap already present in badge's own test file (root tsconfig doesn't include jest-dom matcher types), not a regression. Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree (front-b2b does not currently depend on `@flash-global66/g-radio-group` at all; its `<g-radio-group>` usages resolve to the already-native `GRadioGroup` bundled inside `@flash-global66/g-radio`, unaffected by this WU)

## WU3: skeleton + skeleton-item (PR 3)

- [x] 3.1 Wrote failing tests in `components/skeleton/tests/Skeleton.spec.ts` (12 tests) and `SkeletonItem.spec.ts` (13 tests) — 25 tests total, RED confirmed against old EL-wrapped Skeleton/SkeletonItem.vue (revealed the same pre-existing gap WU1/badge found: outside a ConfigProvider it rendered `el-skeleton*`/`el-skeleton-item*`, not `gui-skeleton*`; 19/25 failed on the class-parity assertions, 6/25 behavioral assertions already passed since the old wrapper faithfully delegated to the real `ElSkeleton`/`ElSkeletonItem`)
- [x] 3.2 Rewrote `components/skeleton/Skeleton.vue` and `SkeletonItem.vue` as native templates (removed `ElSkeleton`/`ElSkeletonItem` + `element-plus` import entirely) — both use `useNamespace('skeleton')` from `@flash-global66/g-utils`, matching EP's own compiled render logic 1:1 (loading/count/rows/template-slot iteration, first/paragraph/last BEM state classes). Ported EP's `useThrottleRender` hook inline (no new shared composable file, matching this package's flat-layout convention) to preserve `throttle` (number or `{leading,trailing,initVal}`) behavior. Added an explicit `loading` prop (default `true`, matching EP's own default) since the old wrapper only worked via implicit `$attrs` fallthrough onto the real `ElSkeleton` — removing that delegation without declaring the prop would have silently broken any consumer passing `loading`/`throttle`, so this was necessary to preserve behavioral (not just formal) parity, same category of judgment call as WU2's latent-bug fix. `SkeletonItem`'s `image` variant inlines EP's `PictureFilled` glyph as a raw `<svg>` (same `viewBox`/`path` extracted from `@element-plus/icons-vue`'s bundled source) instead of importing the icon package, preserving the exact rendered glyph with zero element-plus dependency
- [x] 3.3 Ported `components/skeleton/skeleton.styles.scss` and `skeleton-item.styles.scss` byte-exact off `element-plus/theme-chalk/src/{skeleton,skeleton-item}.scss` — re-pointed `@use` to `@flash-global66/g-utils/{mixins,var-mixins,tokens}`; selectors/values/keyframes preserved byte-exact. `$skeleton` token map already existed in `g-utils/styles/tokens.scss` from `ep-extraction-scss` (same pattern WU1/badge found for `$badge`), and the `avatar` size CSS var chain (`css-var-from-global(('skeleton','circle-size'), ('avatar','size'))`) already resolves via the existing `$avatar-size` map. Preserved the pre-existing `.gui-skeleton__item { --gui-skeleton-color: #eee; }` override that was already shipped in the old wrapper's `skeleton-item.styles.scss`. Verified both files compile cleanly via `sass.compile` (same import-resolution approach as `scripts/scss-parity.mjs`) producing byte-identical `gui-*` selectors/values vs the EP source (only the namespace substitution differs, as expected)
- [x] 3.4 Removed `components/skeleton/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/skeleton/**/*.{ts,vue}'` clean (full-repo `yarn lint` has 65 pre-existing errors unrelated to this WU, none in `components/skeleton/**`, confirmed via grep)
- [x] 3.5 `yarn test:run` green (25/25 skeleton, 367/367 executable tests full suite; 5 pre-existing unrelated test **files** fail to resolve `@flash-global66/g-form`/`g-icon-font` — same fresh-worktree unbuilt-dist gap WU1/WU2 already documented, confirmed unrelated: `components/icon-button`, `components/checkbox`, `components/button`, `components/radio-group` — none touch skeleton). `npx vue-tsc --noEmit -p .` clean on `Skeleton.vue`/`SkeletonItem.vue` production source; remaining vue-tsc errors on `Skeleton.spec.ts`/`SkeletonItem.spec.ts` are the same pre-existing systemic jest-dom-matcher-type gap already present in badge/radio-group's own test files (root tsconfig doesn't include jest-dom matcher types), not a regression. Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge (same deferral as WU1/WU2)

## WU4: infinite-scroll (PR 4)

- [x] 4.1 Write failing tests in `components/infinite-scroll/tests/InfiniteScroll.spec.ts` for the ported directive's scroll-trigger/disabled/distance/immediate/cleanup behavior — 9 tests, RED confirmed (module `../src/infinite-scroll.directive` did not exist yet); covers: trigger near bottom threshold, no-trigger while far, `infinite-scroll-disabled` (literal + EP's attribute-value-as-reactive-instance-key contract), `infinite-scroll-delay` throttling (leading+trailing coalescing), `infinite-scroll-immediate` (fires once on mount when content underfills container, and opt-out via `="false"`), listener cleanup on unmount, and the `isFunction` guard throwing a scoped error
- [x] 4.2 Ported EP's `v-infinite-scroll` directive from scratch (`es/components/infinite-scroll/src/index.mjs`, element-plus 2.9.7) into `components/infinite-scroll/src/infinite-scroll.directive.ts` (replaced the bare `ElInfiniteScroll` re-export) — preserves mounted/updated/unmounted lifecycle, `getScrollOptions` attribute-reading contract, `handleScroll`/`checkFull` trigger algorithm byte-behavior-exact; `getScrollContainer`/`getOffsetTopDistance` DOM helpers ported locally into `src/utils/scroll.util.ts` (reusing `@flash-global66/g-utils`'s `isClient`/`getStyle`) and a small leading+trailing `throttle` ported into `src/utils/throttle.util.ts` (no new runtime dependency — avoided adding `lodash-unified` to keep this "isolated" WU's footprint minimal)
- [x] 4.3 Export the DS directive from `components/infinite-scroll/index.ts` as `GInfiniteScroll` (same export name — existing `v-infinite-scroll="loadMore"` consumers unaffected)
- [x] 4.4 Removed `components/infinite-scroll/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/infinite-scroll/**/*.{ts,vue}'` clean; full `yarn lint` shows 65 pre-existing errors unrelated to this WU (stories, search-input, table — none in infinite-scroll)
- [x] 4.5 `yarn test:run` green (9/9 infinite-scroll; 377/377 passing across the full suite — 5 unrelated pre-existing suite-load failures in button/icon-button/checkbox/radio-group from unbuilt `@flash-global66/g-icon-font`/`g-form` dist in this fresh worktree, same class of gap already documented in WU1/WU2/WU3 progress, not a regression). `npx vue-tsc --noEmit -p .` clean on all infinite-scroll files. Verified `components/infinite-scroll/package.json` declares `@flash-global66/g-utils` (the only new DS dependency actually imported) — the exact missing-dependency bug class caught in WU2/WU3 by adversarial review, checked proactively this time. Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge

## WU5: form-item (PR 5)

- [x] 5.1 Wrote failing tests in `components/form-item/tests/FormItem.spec.ts` (14 tests) for label/prop rendering, required asterisk (explicit prop + rule-derived), validation wiring via DS `g-form` context, trigger filtering (blur/change), error display per `showMessage` mode, `resetField`/`clearValidate`, and a real `@flash-global66/g-input` interop handshake — RED confirmed against old EL-wrapped FormItem.vue (module didn't yet import `@flash-global66/g-form`'s context keys)
- [x] 5.2 Rewrote `components/form-item/FormItem.vue` as a native template ported from element-plus's real `ElFormItem` (label/labelWidth/labelPosition/required/for/inlineMessage/size support, previously only reachable via `$attrs` fallthrough onto the real `ElFormItem`), removed `ElFormItem`/`element-plus` import entirely. Validation state machine (`normalizedRules`/`doValidate`/`validate`/`resetField`/`clearValidate`/`addInputId`/`removeInputId`) ported from `@flash-global66/g-form`'s own already-native `form-item.vue` (itself a simplified EP port), extended with the label/required-asterisk/size template layer EP has and g-form's internal one omits. `showMessage` keeps g-form's native `'parent'|'child'|'none'` semantics (matching every other already-migrated DS input) rather than EP's boolean, since the prop was previously dead/commented code with no real "before" behavior to preserve
- [x] 5.3 Re-pointed to DS `formContextKey`/`formItemContextKey` imported directly from `@flash-global66/g-form` (Symbol.for global registry) — **fixes a real latent bug**, not just an import formality: every production consumer nests `<GFormItem>` (this package) inside `<GForm>` (a separate `@flash-global66/g-form` package); the previous EP-keyed `formContext` was always `undefined` at runtime since it's a different symbol, so `formContext?.addField(...)` was a silent no-op and `formRef.value.validate()` never actually validated fields rendered through this package. Verified via `components/form/src/form-item.vue`/`Form.vue`/`constants.ts` (g-form's own native FormItem, context shape, and Symbol keys) and confirmed zero front-b2b consumer uses `@flash-global66/g-form-item` alongside anything other than `<GForm>` from `@flash-global66/g-form` (grep across `../front-b2b/src`)
- [x] 5.4 Removed `components/form-item/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/form-item/**/*.{ts,vue}'` clean (full-repo `yarn lint` has 336 pre-existing errors unrelated to this WU, confirmed via grep — all in `stories/`, `search-input`, `table`)
- [x] 5.5 `yarn test:run` green (14/14 form-item, 424/424 full suite — required building `@flash-global66/g-form`/`g-input`/`g-icon-font` dist locally in this fresh worktree, same class of gap already documented in WU1-4 progress, not a regression). `npx vue-tsc --noEmit -p .` clean on `FormItem.vue`/`form-item.type.ts`/`index.ts` production source; remaining vue-tsc errors on `FormItem.spec.ts` are the same pre-existing systemic jest-dom-matcher-type gap already present in every prior WU's test file (root tsconfig doesn't include jest-dom matcher types), not a regression. `form-item.styles.scss` ported off `element-plus/theme-chalk/src/form-item.scss` (deep selectors targeting EP's own input-family classes dropped — no longer relevant, every migrated DS input owns its own `is-error` visual state), compiled cleanly via `sass.compile` with the repo's own `@flash-global66/*` package-export importer. Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge (same deferral as WU1-4)

## WU6: popover (PR 6)

- [x] 6.1 Wrote failing tests in `components/popover/tests/Popover.spec.ts` (17 tests) covering: mount/reference-slot render, zero element-plus classes, default-slot vs `content`-prop fallback, `title` rendering, all 3 trigger modes (click/hover/focus), `placement`, `disabled`, `width` inline style, `v-model:visible` (controlled + `update:visible(false)` on close), before-enter/show + before-leave/hide/after-leave lifecycle-event pairing, and 3 real (non-mocked) interop checks against `@flash-global66/g-tooltip`/`g-dropdown`'s actual prop descriptors plus a real nested `<button>` reference element. RED confirmed against the pre-change EP-wrapped `Popover.vue` (materialized via `git show origin/main:components/popover/Popover.vue` into a physically separate scratch file per the WU5 worktree-old-vs-new lesson — never trust relative imports from inside a worktree that already has new code checked out on that path)
- [x] 6.2 Ported `components/popover/Popover.vue` off `ElPopover`/`element-plus`, rendering `<g-tooltip>` internally — mirrors element-plus's own real `popover.vue`/`popover2.mjs` (read directly from `node_modules/element-plus@2.9.7`): computed `kls`/`style`/`gpuAcceleration`, own `#content` slot rendering the `title` div + default-slot-with-content-fallback (NOT delegating to `g-tooltip`'s own built-in title/description/closable content UI, which is a different, DS-specific feature), `before-show`/`before-hide`/`show`/`hide` event mapping to the package's pre-existing public `before-enter`/`before-leave`/`show`/`after-enter`/`hide`/`after-leave` contract (confirmed empirically which OLD events carried a payload vs not, and preserved both `after-enter`(no payload)/`show`(payload) firing together from the same tooltip toggle, matching the OLD wrapper's dual-emission byte-for-byte), `onUpdate:visible` prop-capture-and-reforward technique (identical to EP's own `props[updateEventKeyRaw]` trick), and `hide()`/`popperRef` exposed. Zero `element-plus` import
- [x] 6.3 Re-pointed `trigger`/`disabled` to `@flash-global66/g-tooltip`'s own `useTooltipTriggerProps`; `visible`/`content`/`teleported`/`enterable`/`effect` to its `useTooltipContentProps`; `placement`/`tabindex`/`popperOptions` to `@flash-global66/g-dropdown`'s own `dropdownProps` — confirmed these are the SAME real prop descriptors (not lookalikes) via 2 dedicated interop tests. Found and fixed one real interop gotcha empirically (NOT a Symbol/context-key mismatch like WU5's, but a prop-shape one): `g-tooltip`'s own `tooltip.vue` builds its final `popper-class` via naive template-string interpolation (`` `${ns.e('tooltip')} ${popperClass ?? ''}` ``) instead of Vue's `normalizeClass`, so passing the array/object EP's real `popover.vue` uses for `kls` produced a literal `"gui-popover,,[object Object]"` class (caught by the `width`-inline-style test, which queries the same DOM node). `g-dropdown`'s own already-shipped `<g-tooltip>` usage independently confirms the workaround: it only ever passes a plain string to `popper-class`. Fixed by pre-normalizing with Vue's `normalizeClass()` before handing the class down
- [x] 6.4 Removed `components/popover/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/popover/**/*.{ts,vue}'` clean (full-repo `yarn eslint .` has 851 pre-existing errors unrelated to this WU — confirmed via grep, only match is the pre-existing, untouched `stories/popover.stories.ts`). Declared the 3 new DS dependencies actually imported (`@flash-global66/g-tooltip`, `@flash-global66/g-dropdown`, `@flash-global66/g-utils`) in `components/popover/package.json` — the exact missing-dependency bug class WU3 caught, checked proactively
- [x] 6.5 `yarn test:run` green (17/17 popover, 428/428 full suite — required building `@flash-global66/g-tooltip`/`g-dropdown`/`g-popper`/`g-form`/`g-focus-trap`/`g-slot`/`g-teleport`/`g-collection`/`g-roving-focus-group`/`g-icon-font`/`g-scrollbar`/`g-icon-button` dist locally in this fresh worktree, same class of gap already documented in every prior WU. 1 pre-existing unrelated suite-load failure: `components/form-item/tests/FormItem.spec.ts` needs `@flash-global66/g-input` dist, untouched by this WU). `npx vue-tsc --noEmit -p .` clean on `Popover.vue`/`popover.styles.scss`-adjacent production source; remaining vue-tsc errors on `Popover.spec.ts` are the same pre-existing systemic jest-dom-matcher-type gap already present in every prior WU's test file. `popover.styles.scss` ported byte-exact off `element-plus/theme-chalk/src/popover.scss` (verified via direct `sass.compile` diff against the OLD file's compiled output — only the `el-`→`gui-` namespace substitution differs, as expected; `$popover` token map already existed in `g-utils/styles/tokens.scss` from `ep-extraction-scss`). Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge (same deferral as WU1-5); front-b2b's one real consumer (`src/modules/multiTransactionV2/components/menu-multi-transaction/index.vue`) relies on `width`/`trigger`/`placement`/`show-arrow`/`offset`/`@before-enter`/`@before-leave`/`#reference` slot/default slot/consumer `class` forwarding onto the wrapper div — all preserved and empirically verified (the `class`-forwarding behavior specifically was verified against the OLD component via a throwaway probe test, confirming the class lands on `.gui-popover-wrapper`, which is why that wrapper div was deliberately kept rather than dropped)

## WU7: menu family — Menu/MenuItem/MenuSub/MenuItemGroup (PR 7)

**Size note (pre-approved `size:exception`)**: this unit lands at ~2,000 changed lines (well above the 400-line budget and above even the 500–800 pre-estimate). This is EXPECTED, not scope creep — the line count is the sum of: (1) one new shared context module (`constants.ts`, ~120 lines) that all 4 components import per Design Decision 1; (2) 4 fully-ported components, each replacing a ~15-line EP-wrapping shim with a genuine native port of element-plus's real render/state-machine logic (`Menu.vue`/`MenuSub.vue` alone carry the shared provide/inject state machine — open/close/select, `uniqueOpened`, `mode`, `collapse`, popup-vs-inline rendering, ported collapse-transition animation hooks); (3) a byte-ported `menu.styles.scss` off 4 element-plus theme-chalk source files (~380 lines); and (4) 5 test files (~750 lines) covering each component individually PLUS the mandatory real-mount, non-mocked, 4-component integration suite this WU's highest-risk classification explicitly required. Splitting any of this across multiple PRs would break the provide/inject symbol-identity handshake (Design Decision 1's own rationale) or ship components with no test coverage in between — so it stays one PR by design.

- [x] 7.1 Write failing tests for provide/inject handshake (open/close/select/collapse) across all 4 components — 31 tests across 5 files (`Menu.spec.ts` 11, `MenuItem.spec.ts` 6, `MenuSub.spec.ts` 6, `MenuItemGroup.spec.ts` 2, `Integration.spec.ts` 6); RED confirmed incrementally against the old EL-wrapped components while `constants.ts` + the 4 native ports were being written test-first component by component (Menu → MenuItem → MenuSub → MenuItemGroup → Integration)
- [x] 7.2 Create one shared context module (`components/menu/constants.ts`) with `menuContextKey`/`subMenuContextKey` as plain `Symbol()` (NOT `Symbol.for`, deliberately — this package has no cross-package interop need unlike WU2's radio-group, so a global-registry key would only add an unwanted global side effect), imported by parent and all 3 descendants (Design Decision 1 — one symbol identity, never redeclared). Includes `assertMenuContext` — a fail-fast helper that throws a descriptive error (matching element-plus's own `throwError`) instead of silently rendering with `undefined` context, so a symbol-identity handshake break (the WU5 lesson) surfaces immediately as a thrown error in tests, not a silent runtime no-op
- [x] 7.3 Ported `components/menu/Menu.vue` off `ElMenu` onto the shared context module — full state machine (`activeIndex`, `openedMenus`, `items`/`subMenus` registries, `openMenu`/`closeMenu`/`handleMenuItemClick`/`handleSubMenuClick`, `uniqueOpened` sibling-closing, `initMenu` ancestor auto-open on mount, router-mode via `appContext.config.globalProperties.$router` same technique element-plus itself uses — no static `vue-router` dependency). Documented, deliberate scope cuts (not silent): the horizontal ellipsis/"more" overflow feature (ResizeObserver DOM measurement, ellipsisIcon) and the width-fade `collapseTransition` cosmetic animation are NOT ported — `../front-b2b`'s only real consumer (`MenuRoute/index.vue`) already passes `:collapse-transition="false"` and never uses horizontal mode, so both cuts are zero-behavior-change for the one real production usage
- [x] 7.4 Ported `components/menu/MenuItem.vue`, `MenuSub.vue`, `MenuItemGroup.vue` off `ElMenuItem`/`ElMenuSub`/`ElMenuItemGroup`. `MenuSub.vue` carries the two rendering branches EP itself has (inline accordion for `mode="vertical"` without `collapse` — ported EP's own `ElCollapseTransition` maxHeight/padding JS hooks inline, since the CSS class hooks already existed in `g-utils/styles/transition.scss` from `ep-extraction-scss`; popup via a real `<g-tooltip>` for `horizontal`/collapsed modes, re-pointing `placement`/`offset`/`teleported`/`popperClass`/`transition` the same way WU6/popover already proved works). `MenuItemGroup.vue` is deliberately non-participating in provide/inject (no `provide()` call) — items nested inside a group still resolve the correct ancestor context via Vue's own inject bubble-through, matching element-plus's real behavior where a group never appears in `indexPath` (verified empirically in `Integration.spec.ts`). Fixed 2 pre-existing copy-paste bugs found while porting: `MenuSub.vue`/`MenuItemGroup.vue`'s internal component `name` was `'GMenuItem'` in both (copy-pasted from `MenuItem.vue`) — corrected to `'GSubMenu'`/`'GMenuItemGroup'`; `MenuItemGroup`'s dead `index`/`route`/`disabled` props (copy-pasted, never part of element-plus's real `ElMenuItemGroup` contract, confirmed unused by any `../front-b2b` consumer) were dropped
- [x] 7.5 Removed `components/menu/**` from `.eslintrc.cjs` excludedFiles; `npx eslint --max-warnings 0 'components/menu/**/*.{ts,vue}'` clean (full-repo `yarn eslint .` shows 845 pre-existing errors unrelated to this WU — confirmed via grep, zero matches under `components/menu/`). Declared the 2 new DS dependencies actually imported (`@flash-global66/g-tooltip`, `@flash-global66/g-utils`) in `components/menu/package.json` — the exact missing-dependency bug class caught in WU2/WU3, checked proactively. **`yarn.lock` explicitly verified and regenerated** (WU6's lesson — a stale lockfile missing the new deps' `dependencies:` block would have broken CI's `yarn install --immutable`): confirmed the gap existed (`g-menu`'s lockfile entry had no `dependencies:` key), ran `yarn install`, then re-verified with `yarn install --immutable` (passes clean, zero drift)
- [x] 7.6 `yarn test:run` green (31/31 menu — 5 files, including the real-mount `Integration.spec.ts`; 477/477 full suite — required building `@flash-global66/g-tooltip`/`g-popper`/`g-focus-trap`/`g-slot`/`g-teleport`/`g-icon-button`/`g-collection`/`g-roving-focus-group`/`g-icon-font`/`g-scrollbar`/`g-form`/`g-input`/`g-dropdown` dist locally in this fresh worktree, same class of gap already documented in every prior WU — none of these are new to this WU's scope, `g-input`/`g-dropdown` were only needed to un-block 2 unrelated pre-existing suites (`form-item`, `popover`) also affected by the fresh-worktree gap, confirmed unrelated to this change). `npx vue-tsc --noEmit -p .` clean on all 5 production files (`Menu.vue`/`MenuItem.vue`/`MenuSub.vue`/`MenuItemGroup.vue`/`constants.ts`); remaining vue-tsc errors are on the `.spec.ts` test files only — the same pre-existing systemic jest-dom-matcher-type gap already documented in every prior WU's test files, not a regression. `menu.styles.scss` ported off `element-plus/theme-chalk/src/menu.scss` (2.9.7; `sub-menu.scss`/`menu-item.scss`/`menu-item-group.scss` are empty in this EP version, consolidated into `menu.scss` — nothing to port from them), re-pointed to `@flash-global66/g-utils/{mixins,var-mixins,tokens,transition}`; `$menu` token map and the `gui-zoom-in-top`/`gui-zoom-in-left`/`gui-collapse-transition`/`gui-opacity-transition` transition classes this WU's popup/accordion animations rely on already existed from `ep-extraction-scss`; verified via direct `sass.compile` (zero errors). Live `yarn-link` validation in `../front-b2b` deferred — human-in-the-loop step per orchestrator scope for this isolated worktree, pending before PR merge (same deferral as WU1-6); the real consumer (`src/shared/layouts/components/MenuRoute/index.vue`) uses `GMenu`(`collapse`, `collapse-transition=false`, `default-active`, `router`) + `GMenuItem`(`index`, `title`/`default` slots) + `GSubMenu`(`index`, `title`/`default` slots) nested up to 2 levels — no `GMenuItemGroup` usage in production, but still fully ported/tested per WU scope (all 4 components)

## WU8: time-picker clearIcon default fix (PR 8)

- [ ] 8.1 Write failing test asserting `clearIcon` defaults to `true` in `components/time-picker/src/common/props.ts`
- [ ] 8.2 Edit `components/time-picker/src/common/props.ts:4` — delete `import { CircleClose } from '@element-plus/icons-vue'`
- [ ] 8.3 Edit `components/time-picker/src/common/props.ts:88-91` — `clearIcon` default becomes `true` (boolean); keep type `string | Component` (no breaking prop-type change); leave `picker.vue` untouched
- [ ] 8.4 `yarn test:run` green; yarn-link validate live in `../front-b2b`

## WU9: config-provider — LAST DS unit (PR 9)

- [ ] 9.1 Write failing tests for `provideGlobalConfig`-from-g-hooks context shape parity (dialog `namespace='gui'`; `emptyValues` consumers untouched — Design Decision 2)
- [ ] 9.2 Edit `components/config-provider/ConfigProvider.vue` — call DS `provideGlobalConfig({ namespace: 'gui' })` from g-hooks; keep `provide(namespaceContextKey, 'gui')`; remove EP `provideGlobalConfig` import
- [ ] 9.3 Switch `components/config-provider/config.styles.scss` to DS-owned brand-color `--gui-*` emission (closes scss-token-foundation task 4.5); zero element-plus involvement
- [ ] 9.4 Drop `@use "element-plus/theme-chalk/src/base.scss"` from `components/time-picker/src/common/time-picker.styles.scss` (now safe — config-provider owns brand emission)
- [ ] 9.5 Remove `components/config-provider/**` from `.eslintrc.cjs` excludedFiles — MUST be the last of the 8 v6 exclude removals
- [ ] 9.6 `yarn test:run` green; yarn-link validate live in `../front-b2b`; confirm dialog and time-picker rendering unchanged

## WU10: root element-plus removal + g-hooks cleanup (PR 10)

- [ ] 10.1 Gate check: `grep -rE "from ['\"](element-plus|@element-plus/icons-vue)"` across `components/**/src` and `components/**/index.ts` returns zero matches
- [ ] 10.2 Remove `"element-plus": "2.9.7"` from root `package.json:62`; prune `yarn.lock`
- [ ] 10.3 Edit `common/g-hooks/tests/composables/useGlobalConfig.spec.ts:5` — remove `import { configProviderContextKey } from 'element-plus'`; adjust the "no coupling" assertion to compare against the DS key only
- [ ] 10.4 Confirm `.eslintrc.cjs` excludedFiles no longer lists any of the 8 v6 packages
- [ ] 10.5 `yarn install` and `yarn build` succeed with zero `element-plus` resolution workspace-wide; `yarn test:run` green

## WU11: front-b2b version bumps (PR 11)

- [ ] 11.1 Bump `../front-b2b/package.json` deps to published Lerna versions: `g-badge`, `g-config-provider`, `g-form-item`, `g-infinite-scroll`, `g-menu`, `g-popover`, `g-skeleton`, `g-time-picker` (NOT `g-radio-group`)
- [ ] 11.2 `yarn install` in `../front-b2b`; confirm no unpublished/local-only version is pinned
- [ ] 11.3 Build `../front-b2b`; confirm no regression in the 8 migrated components' consuming screens

## WU12: front-b2b element-plus removal (PR 12)

- [ ] 12.1 Remove `"element-plus": "2.9.7"` from `../front-b2b/package.json:110`
- [ ] 12.2 Remove the `element-plus` entry from `../front-b2b/vite.config.ts:102` `optimizeDeps.include`
- [ ] 12.3 `yarn install` and build `../front-b2b`; confirm zero `element-plus` resolution and no regressions
